# Copilot Code Review Instructions

Read and follow the repository-root `AGENTS.md`, which is canonical.

Never bypass any required status check or required review.

Review this security-sensitive CLI plugin carefully; ensure JSON schemas and `validateStructuredReviewOutput` stay in sync.
