---
description: List durable Codex-Claude Bridge jobs and their current lifecycle state.
---

# /claude-review:list

Run `codex-claude list <user-arguments>`. Prefer `--json` when another Codex step
will consume the result. Report job IDs, workspaces, lifecycle state, and any
recovery-required condition without inferring success from a missing tmux pane.
